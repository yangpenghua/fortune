# 修改项目端口 — 任务分解

**日期**：2026-04-08
**服务**：AI算命应用

## Task 1：修改 package.json 端口配置

**涉及文件**：
- `package.json`（修改）

**子步骤**：
1. 修改 `dev` 脚本，从 `"dev": "next dev"` 改为 `"dev": "next dev -p 9527"`

**验收标准**：
- [ ] 文件修改正确
- [ ] JSON 语法无误

---

## Task 2：重启开发服务器验证

**涉及文件**：无

**子步骤**：
1. 停止当前运行的开发服务器（3000端口）
2. 重新执行 `npm run dev`
3. 验证服务在 9527 端口启动

**验收标准**：
- [ ] 终端显示 "Local: http://localhost:9527"
- [ ] 浏览器访问 http://localhost:9527 正常打开
- [ ] 应用功能正常使用

---

> 注：此为简单配置变更，无需 Entity、Service、Controller 等层级改动。
