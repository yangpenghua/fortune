# 开发日志 - 2026-04-08

## 完成内容

将项目端口从默认的 3000 修改为 9527。

### 变更文件列表

| 文件 | 变更类型 | 说明 |
|------|---------|------|
| package.json | 修改 | dev 脚本添加 `-p 9527`，start 脚本添加 `-p 9527` |
| .env | 修改 | NEXT_PUBLIC_SITE_URL 从 `http://localhost:3000` 改为 `http://localhost:9527` |
| docker-compose.yml | 修改 | 端口映射从 `3000:3000` 改为 `9527:3000`；NEXT_PUBLIC_SITE_URL 默认值更新 |
| README.md | 修改 | 文档中访问地址从 `http://localhost:3000` 改为 `http://localhost:9527` |

## 遇到的问题 & 解决方案

### 问题1：配置文件不一致
- **问题描述**：代码审查发现多个配置文件中仍使用 3000 端口，导致配置不一致
- **解决方案**：同步更新所有相关配置文件，包括 .env、docker-compose.yml、README.md

## 数据库变更提醒

无数据库变更。

## 部署注意事项

- 开发环境：执行 `npm run dev` 将在 http://localhost:9527 启动
- Docker 部署：端口映射已更新为 9527:3000
- 生产部署：如需指定生产端口，可通过环境变量 `PORT=9527 npm start`